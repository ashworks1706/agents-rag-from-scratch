# Modern RAG in Practice — workshop dependencies
# Pinned loosely for teaching; Streamlit Cloud resolves compatible builds.

streamlit>=1.36
pymupdf>=1.24            # PDF text extraction (imported as fitz)
sentence-transformers>=3.0  # all-MiniLM-L6-v2 embeddings
numpy>=1.26
google-genai>=0.3        # Gemini answer generation

# CPU-only PyTorch keeps the Streamlit Cloud build small and fast.
# If the default torch pull is too large on first deploy, uncomment:
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch
